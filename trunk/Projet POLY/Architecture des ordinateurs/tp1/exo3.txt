#include <stdio.h>
#include <math.h>
#include <string.h>
int main (int argc, const char * argv[]) {
	char nom[50],prenom[50];
	int taille = strlen(nom);
	printf("Quel est ton nom connard ? ");
	scanf("%50s %50s",prenom,nom);
	
	printf("Bonjour connard ! Tu t'appelles %s, et ton nom de connard comporte %d lettre",prenom,taille);
    return 0;
}

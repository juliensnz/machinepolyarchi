#include <stdio.h>

int main (int argc, const char * argv[]) {
	int val = 0;
	int cpt = 0;
	int somme = 0;
	while(cpt<40)
	{
		if(val % 2 == 1)
		{
			cpt++;
			somme += val;
			printf("somme : %d, valeur : %d\n",somme,val);
		}
		val++;
	}
    return 0;
}

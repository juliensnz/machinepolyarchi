#include <stdio.h>
#include <math.h>

int main (int argc, const char * argv[]) {
	float a,b,c,x1,x2,delta;
	int test;
	printf("a : ");
	scanf("%f",&a);
	printf("b : ");
	scanf("%f",&b);
	printf("c : ");
	scanf("%f",&c);
	delta = pow(b,2)-4*a*c;
	if(delta > 0)
	{
		x1 = (-b+sqrt(delta))/-2*a;
		x2 = (-b-sqrt(delta))/-2*a;
		printf("resultat : x' = %f , x'' = %f",x1,x2);
	}
	else if(delta == 0)
	{
		x1 = (-b)/-2*a;
		printf("resultat : %f",x1);
	}
	
    return 0;
}
